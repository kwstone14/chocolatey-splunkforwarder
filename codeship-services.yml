app:
  build:
    image: microsoft/powershell
    dockerfile: Dockerfile
  cached: true
  volumes_from:
    - data
  environment:
    ENV: string
    ENV2: string
data:
  image: busybox
  volumes:
    - ./tmp/data:/data
